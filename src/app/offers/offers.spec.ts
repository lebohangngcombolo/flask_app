import { TestBed } from '@angular/core/testing';
import { OffersComponent } from './offers';

describe('OffersComponent', () => {
	it('should create', () => {
		const component = TestBed.createComponent(OffersComponent).componentInstance;
		expect(component).toBeTruthy();
	});
});
